public enum SkillType
{
    Active,
    Buff
}

public enum BossState
{
    Idle,
    Moving,
    Attacking,
    Dashing,
    Dying
}

public enum RewardType
{
    None,
    Item,
    Exp,
    Skill
}

public enum UIEvent
{
    Click,
}
