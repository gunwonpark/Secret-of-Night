using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Marker : MonoBehaviour
{   
    // Update is called once per frame
    void Update()
    {
        if(QuestManager.I.currentQuest.Queststatus == QuestStatus.Wait)
        {

        }
        
    }
}
